# Шаблон переменных окружения. Скопируйте в .env и заполните.
# .env в .gitignore — секреты не коммитятся.

# --- Content Assets tests ---
VRP_MEMBER_EMAIL=
VRP_MEMBER_PASSWORD=

# --- Payment tests ---
# Учётка существующего мембера для Layer 03 (Upgrade/Cancel/Refund) и re-join (Layer 02/05).
VRP_PAY_EMAIL=
VRP_PAY_PASSWORD=
